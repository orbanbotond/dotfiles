#########################################
# Environment specific variables

export CLICOLOR=1;
export EDITOR="subl"
export SVN_EDITOR="subl -w"
export HISTFILESIZE=9000 # the bash history should save 9000 commands
export HISTCONTROL=ignoredups #don't put duplicate lines in the history.
export ANT_HOME=/usr/share/java/ant-1.7.1
export MANPATH=/opt/local/share/man:$MANPATH
export CC=/usr/bin/gcc-4.2

export PATH=$PATH:/usr/sbin:/usr/local/mysql/bin:
export PATH=/opt/local/bin:/opt/local/sbin:$PATH
export PATH=~/bin:$PATH

# p=$(launchctl getenv PATH)
# launchctl setenv PATH /bin:$p

